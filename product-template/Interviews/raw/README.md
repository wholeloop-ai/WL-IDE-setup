# Raw interviews

Place original transcripts here (`.md`, `.txt`, `.pdf`).

**Never edit** files after they are filed. Corrections go in `processed/` synthesis or `risks_and_assumptions`.

Naming: `YYYY-MM-DD-participant-business.md` (optional; agent can rename reference in `source_file`).
