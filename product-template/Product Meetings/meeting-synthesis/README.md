# Meeting synthesis

One YAML per meeting: `YYYY-MM-DD-<slug>.yaml`

Schema: `_schema.yaml` v1.0

Skill: `synthesize-meeting` may create `Roadmap/feature-<slug>.md` when `roadmap_action: create_brief`.
