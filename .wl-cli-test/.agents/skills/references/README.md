# references/

`project-conventions.md` is **generated** by the WholeLoop CLI (`wholeloop init` or `wholeloop conventions bootstrap`), then completed with the **project-conventions** agent in your IDE.

Do not commit placeholder-only files without team review.
